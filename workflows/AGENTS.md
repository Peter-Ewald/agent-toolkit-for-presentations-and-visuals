# Workflow Agent Notes

Use this folder when the task is phrased as a concrete deliverable.

Read first:

- `README.md`
- `../docs/workflow-layer.md`

Routing:

- `presentations/` for deck-only recipes
- `visualisations/` for visual-only recipes
- `combined/` for workflows that touch both layers
